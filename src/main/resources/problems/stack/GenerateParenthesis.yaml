name: GenerateParenthesis
description: |
  You are given an integer `n`. Return all well-formed parenthesis strings that you can generate with `n` pairs of
  parenthesis.
difficulty: Medium
tags:
  - stack
template:
  name: SingleFunction
  args:
    input:
      - name: n
        type: int
    output: List<String>
